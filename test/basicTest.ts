import { expect } from "chai";
import "@nomiclabs/hardhat-ethers";
import { ethers } from "hardhat";

describe("Greeter", function () {
  it("Should return the new greeting once it's changed", async function () {

	
    // const Greeter = await ethers.getContractFactory("Greeter");
    // const greeter = await Greeter.deploy("Hello, world!",100);
    // await greeter.deployed();

    // expect(await greeter.greet()).to.equal("Hello, world!");
	// expect(await greeter.getX()).to.equal(100);

	// const setGreetingTx = await greeter.setGreeting("Hola, mundo!");
    // // wait until the transaction is mined
    // await setGreetingTx.wait();
    // expect(await greeter.greet()).to.equal("Hola, mundo!");

	// const setXTx = await greeter.setX(101);
	// await setXTx.wait();
    // expect(await greeter.getX()).to.equal(101);

	// let [acc0, ...accs] = await ethers.getSigners()
	// console.log(acc0.address);

	// const acc0eth = await acc0.getBalance();
	// console.log(acc0eth);
	
  });
});
